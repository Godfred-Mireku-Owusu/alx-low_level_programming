#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'the description here'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int alph;

	for (alph = 'a'; alph <= 'z'; alph++)
		putchar(alph);
	return (0);
}
